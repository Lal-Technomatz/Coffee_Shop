<%= form_with(model: item) do |form| %>
  <% if item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
        <% item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.number_field :price %>
  </div>

  <div>
    <%= form.label :tax_percent, style: "display: block" %>
    <%= form.collection_select(:tax_category_id, TaxCategory.all, 'id', 'tax_rate',  {prompt: 'Select Tax Value'}) %>
  </div>

  <div>
    <%= form.label :availability, style: "display: block" %>
    <%= form.check_box :availability %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
